{
  "name": "coinglass-vercel-api",
  "version": "1.0.0",
  "description": "Vercel API endpoint for Coinglass Chrome extension",
  "main": "api/data.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.0.1"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
